import React from 'react';

const ClientDashboard = () => (
  <div style={{ padding: '2rem' }}>
    <h1>👤 Client Dashboard</h1>
    <p>This page will show client job statuses, requests, and account info.</p>
  </div>
);

export default ClientDashboard;
