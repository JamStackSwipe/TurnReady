import React from 'react';

const Profile = () => (
  <div style={{ padding: '2rem' }}>
    <h1>👤 Profile</h1>
    <pThis page will include account info, contact methods, and service preferences.</p>
  </div>
);

export default Profile;
